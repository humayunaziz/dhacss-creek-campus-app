name: dhacss_creek_campus_app
description: A polished, offline-first DHACSS Creek Campus parent app prototype.
publish_to: none
version: 0.2.0+2
environment:
  sdk: '>=3.8.0 <4.0.0'
dependencies:
  supabase_flutter: 2.17.2
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
